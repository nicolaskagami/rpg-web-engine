const Entity = require('./entity');
class Condition extends Entity
{
    constructor({object, target, expression, effect})
    {
        super({object: object});
        if(object == null)
        {


        }
    }
}



module.exports = Condition;
